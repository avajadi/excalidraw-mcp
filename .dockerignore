# Build artifacts and deps are produced inside the image, never copied in.
node_modules
web/node_modules
dist
web/dist

# Local state and editor/VCS noise.
scenes
.git
.gitignore
.idea
*.log
npm-debug.log*

# Docker files themselves don't belong in the build context payload.
Dockerfile
.dockerignore
docker-compose.yml
README.md